package dev.realme.ash.api.config;

public enum NumberDisplay {
   DEFAULT,
   DEGREES,
   PERCENT;

   // $FF: synthetic method
   private static NumberDisplay[] $values() {
      return new NumberDisplay[]{DEFAULT, DEGREES, PERCENT};
   }
}
